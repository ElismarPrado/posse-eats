import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    error:{
        fontSize: 10,
        width: '70%',
        color: '#999',
    },

    endereco:{
        maxWidth: '80%',
        fontSize: 9,
        color: '#555',
    }
})

export default styles